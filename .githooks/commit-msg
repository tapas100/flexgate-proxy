#!/bin/sh
# Remove Co-authored-by: Copilot lines from commit messages automatically
sed -i '' '/^Co-authored-by: Copilot.*/d' "$1"
# Remove any trailing blank lines left behind
sed -i '' -e '/^[[:space:]]*$/{ /./!d; }' "$1"
